import { AdminMarketClient } from '@/components/admin/AdminMarketClient';

export const metadata = { title: 'Market Controls — Admin — SmartInvest' };

export default function AdminMarketPage() {
  return <AdminMarketClient />;
}
