import { AdminAnalyticsClient } from '@/components/admin/AdminAnalyticsClient';

export const metadata = { title: 'Analytics — Admin — SmartInvest' };

export default function AdminAnalyticsPage() {
  return <AdminAnalyticsClient />;
}
